import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#fe6b00', // Cambia este valor al color que desees
    },
    secondary: {
      main: '#ff0000', // También puedes cambiar el color secundario
    },
  },
});

export default theme;